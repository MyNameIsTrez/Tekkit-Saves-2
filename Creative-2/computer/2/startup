-- We need to run startup.lua from startup, because startup always gets booted by CC,
-- and VS Code needs the extension name startup.lua to recognize the code as being in LUA,
-- which is useful for extensions which help me debug the code.

shell.run("startup.lua")