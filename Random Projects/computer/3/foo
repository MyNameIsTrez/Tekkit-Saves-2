-- Seems to not be affected by os.pullEvent()
--timer = os.startTimer(2) -- Works.

-- Affected by 
os.queueEvent("lol") -- Doesn't work!


print("start delay")

--sleep(0.05)

--local a = 0
--for i = 1, 1e7 do a = a + 1 end

os.queueEvent("xd")
os.pullEvent("xd")

print("end delay")

-- Works.
--event, timerID = os.pullEvent("timer")
--print(timerID == timer)

-- Doesn't work!
event = os.pullEvent("lol")
print(event == "lol")
